# generated by scripts/to-codex.sh — 정본: .claude/commands/route.md (직접 수정 금지)
# interface.default_prompt 프리필 — $1..$9 인자 치환 부재(#15316)의 공식 우회책.
# Codex가 이 skill을 호출할 때 입력창에 아래 문구를 미리 채운다(L4 완화, CT-SKILL prefill).
interface:
  default_prompt: "$route "
