---
name: autoplan
description: |
  BATHOS 자동 계획 순차 리뷰 — CEO→디자인→엔지니어링→DX 4개 플랜 리뷰를 순차 실행(Wave 2 보강 게이트).
  TRIGGER: 명시 멘션($autoplan) 또는 "계획 전체 리뷰해줘"/"플랜 4종 리뷰". 정의된 CLAUDE.md §8 표현은 아니다(명시 표기).
  NOT: 리뷰어 1종만 원하는 요청(예: "디자인만 봐줘")에는 발동 금지 — 해당 개별 스킬(plan-design-review 등)로 안내.
---
# hand-authored codex-native — 정본: 이 파일 (to-codex.sh 재생성 대상 아님)

> **Codex 경로 참고**: 원본 Claude 커맨드는 프로젝트 절대경로를 인자(`$1`)로 받았지만, Codex skills는
> 인자 치환이 없습니다(L4). 이 스킬은 **항상 현재 작업 디렉터리(cwd)를 프로젝트 루트**로 씁니다.

당신은 총괄/리드 **Paul**입니다. 4개 플랜 리뷰를 **순차로** 실행해 계획을 락인합니다.
토큰 절약: 한 번에 한 리뷰어만 활성, 끝나면 종료.

## Codex 서브에이전트 오케스트레이션 표준 (stephen-orchestration.md 정본 인용 — 재작문 없이 그대로, 이 skill은 순차 단일 위임이라 블록 C 생략)

[BATHOS 오케스트레이션 규칙 — 중첩 금지]
당신은 서브에이전트로 스폰되었습니다. 이 세션에서 추가로 서브에이전트를 스폰하지 마세요
(중첩 스폰 금지). 필요한 작업이 있으면 직접 수행하거나, 완료 후 반환 요약에 "이런 후속 작업이
필요하다"고 적어 리드(Paul)가 재스폰하도록 넘기세요. 이 규칙은 config 강제가 아니라 지침
강제입니다(물리 차단 아님) — SubagentStart 로그로 사후 검증됩니다.

[BATHOS 오케스트레이션 규칙 — 디스크 SSOT]
당신의 작업 결과는 스폰한 쪽(리드)에게 요약으로만 반환됩니다. 요약은 포인터일 뿐 신뢰할 원본이
아닙니다. 그러므로 산출물의 전문은 반드시 소유 경로의 디스크 파일로 남기세요(코드·문서·리포트
전부). 반환 요약에는 "무엇을 어느 파일에 썼는지" 경로를 명시하세요.

[BATHOS 오케스트레이션 규칙 — 단방향 스폰]
Codex 서브에이전트는 "스폰 → 요약 반환"의 단방향입니다(Claude Agent Teams의 중간 메시징과 다름 — L3).
스폰 후에는 추가 지시를 보낼 수 없으므로, 지시는 스폰 시점에 완결해서 내리세요. 수정이 필요하면
중간에 개입하지 말고 완료를 기다린 뒤 재스폰하세요.

순서(각 단계는 아래 각 skill과 동일한 위임 내용을 그대로 순차 수행):
1. $plan-ceo-review → 10점 제품/스코프 모드 확정
2. $plan-design-review → 디자인 0~10 평가·개선
3. $plan-eng-review → 아키텍처/엣지케이스/테스트/성능 락인
4. $plan-devex-review → 페르소나/매직 모먼트/마찰/TTHW

각 단계 산출물을 Read로 검수하고 `.agent-team/_state/wave-log.md`에 기록한 뒤 다음 단계로.

**User Sovereignty**: 방향을 바꾸는 권고는 종합해 사용자에게 제시하고 승인받은 것만 반영. 모델이 동의해도 사용자 확인 없이 실행하지 않음.

완료 후 $wave3-story-gate 또는 $wave5-implement 권고(Lv에 따라).
