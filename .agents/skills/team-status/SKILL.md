---
name: team-status
description: |
  BATHOS 진행 현황 점검 — manifest/wave-log/산출물 인벤토리·다음 커맨드 안내
  TRIGGER: 명시 멘션($team-status)만 — CLAUDE.md §8·커맨드 자체 각주에 자연어 별칭 정의 없음(날조 금지, 미정 상태 정직 표기). 자연어 확장은 실사용 관측 후 사용자 확정을 거쳐 추가한다.
  NOT: 완전 복원 브리핑 의도(→cold-start)에는 발동 금지 — 이 스킬은 경량 점검, cold-start는 3자 교차 완전 복원.
---
# generated by scripts/to-codex.sh — 정본: .claude/commands/team-status.md (직접 수정 금지)

> **Codex 경로 참고**: 원본 Claude 커맨드는 프로젝트 절대경로를 인자(`$1`)로 받았지만, Codex skills는
> 인자 치환이 없습니다(L4 — 이슈 #15316). 이 스킬은 **항상 현재 작업 디렉터리(cwd)를 프로젝트 루트**로
> 씁니다 — 다른 프로젝트를 다루려면 그 디렉터리에서 Codex를 실행하세요. 아래 본문에 원본 표기 `$1`이
> 남아 있으면 전부 **현재 작업 디렉터리**로 읽으세요(치환되지 않습니다 — 지침일 뿐 실제 인자가 아닙니다).

당신은 총괄/리드 **Paul** 입니다. 현재 상태만 점검합니다(스폰·수정 없음).

1. `$1/.agent-team/_state/manifest.json`·`wave-log.md` 읽어 웨이브별 status + 현재 Lv + gate verdicts 요약.
2. `$1/.agent-team/*/` 산출물 존재 여부 점검 → 인벤토리 표(디렉터리 별 주요 파일 유무).
3. W3 게이트 상태 확인 (`03-story-engineering/readiness-report-kr.md` 있으면 verdict 표시).
4. 다음 실행할 웨이브 커맨드와 누락·주의 항목 안내.
5. 활성 팀원이 있으면 역할 목록 보고.

> 변경 없음. 읽기 전용 점검.
